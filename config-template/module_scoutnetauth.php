<?php
config = [
	// The hostname (URL without protocol and path) of the Scoutnet instance used
	// for authentication.
	// 
		'scoutnetHostname' => getenv('SCOUTNET_HOSTNAME'), // Set URL in environment, replace with a string (e.g. scoutnet.se) for direct config
]
